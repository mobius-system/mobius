# 继续优化 Mobius 右侧「会话工具」抽屉（第二轮，必须改代码）

你在仓库 `/Users/boyin.liu/PycharmProjects/mobius-nutshell` 工作。请直接改代码并跑测试，不要只给方案。不要 commit。

## 用户问题

用户附了截图：右侧 230px「会话工具」交互很差。红箭头指向底部「▶ 更多会话动作」。主要痛点：

1. 7 个带文字 tab 排成两行宫格，第二行被截成「编...」。
2. Files / Diff 共用一份内容，底部再用 `<details>更多会话动作</details>` 倾倒整份高级菜单。
3. Header 叠「会话工具」大标题 + 超长路径。
4. 文件路径被截断，预览/浏览/改动列表叠了太多圆角盒子。

必须对齐 CodexMonitor 的**右栏结构纪律**，不是复制皮肤：

- `examples/CodexMonitor/src/features/layout/components/PanelShell.tsx`
- `examples/CodexMonitor/src/features/layout/components/PanelTabs.tsx`
- `examples/CodexMonitor/src/styles/panel-tabs.css`
- `examples/CodexMonitor/src/features/files/components/FileTreePanel.tsx`（文件行：icon + basename 单行，没有时间戳、没有嵌套卡片）

必须学：单行 icon tab + header 右侧动作；每个 tab 只渲染自己的内容；低频动作进 header overflow；文件行显示 basename。

不要学：Git stage/commit/push/PR、Send/Stop 合并、常驻 Git+Plan、改 Harness/SSE/后端、复制 CodexMonitor 色值。

产品边界：`docs/ui-optimization/13-codexmonitor-main-shell-reuse.md` 第 6 条、P1-S1；`docs/ui-optimization/08-capability-presentation.md` 2.1。

## 当前工作区状态（先读再改）

第一轮结构已经部分落地，不要回退：

- `mobius/frontend/src/components/session-tool-drawer.tsx`：已有 icon-only tab、roving tabindex、Arrow/Home/End、`overflow` 槽、`aria-label="更多会话动作"`、细 meta `data-session-tool-source`。
- `mobius/frontend/src/components/chat.tsx`：`toolDrawerContent` 已按 tab 拆开；files/diff 不再渲染 `<details>更多会话动作</details>`；header 已 `overflow={renderAdvancedSessionActions('overflow')}`。
- `mobius/frontend/src/components/advanced-session-actions.tsx`：已有 `variant: 'overflow'`，且不重复一级 tab 入口。
- `mobius/frontend/tests/workbench-simplification-contract.test.js`：已断言上述契约。

**你的任务不是重做第一轮，而是把剩余密度和交互收干净，让 230px 列真的能扫。**

## 本轮必须做的交互简化

### A. 文件行改成 CodexMonitor 式单行

改 `SessionFilesDrawerSurface`（`chat.tsx`）：

- 默认只显示 **basename + count**，完整路径放 `title`（已有 basename 的话保留）。
- **去掉时间戳行**。时间可进 `title`，不要占第二行。
- Files tab **不要**显示「本次改动」大标题和 Diff hint。
- Diff tab 可保留短 hint，文案再缩短（例如「点选后在中心看 Diff」），不要再套一层卡片边框。
- 选中态用 inset accent / `surface-active`，不要再加厚边框盒子。

### B. Files tab 去掉多余盒子

`activeToolTab === 'files'` 时：

- 当前对象改成一行：basename +「预览」，不要再包一张带边框的卡片。
- 「浏览项目文件 / 引用」保持一个扁平按钮，不要再叠圆角卡片。
- 下面直接是改动文件列表。不要在 Files 底部再倾倒任何高级菜单。

### C. Overflow 变轻

`advanced-session-actions.tsx` 的 `variant="overflow"`：

- **保留**现有分组 label：继续方式、运行与协作、知识、Research（契约测试会扫这些字符串）。
- **不要**在 overflow 重复：项目文件、会话文件修改、打开终端、打开编辑器、本会话 Skill 快照、本会话 Memory 快照、Git 仓库状态。
- 视觉上收掉 `ToolMenuGroup` 的厚边框卡片感：overflow 里分组改成小标题 + 扁平按钮列表，hint 可以更短或放到 title。
- `variant="menu"` 必须继续包含全部组（文件、Diff / Git、终端、编辑器、本会话上下文、继续方式、Research），窄屏 popover 还依赖它。

### D. 其它 tab 少一层卡片

terminal / editor / skill / memory / git 保持现有专用表面和能力，但：

- 去掉可有可无的说明卡片边框；说明改成一行 muted 文本即可。
- editor 仍必须 `data-workbench-editor-tool` + `renderAdvancedSessionActions('editor')`。

### E. Tab chrome 继续对齐 PanelTabs

`session-tool-drawer.tsx` + `index.css` 的 `.session-tool-drawer*`：

- 保持 7 个 tab id：`files | diff | terminal | editor | skill | memory | git`
- 保持 `SESSION_TOOL_TAB_LABELS` 中文
- 不要回到 `grid-cols-4` 文字宫格
- 单行 icon pill；中文只在 `aria-label` / `title`
- 键盘：ArrowLeft/Right/Up/Down、Home、End
- `--tool-drawer-width: 230px` 不要改
- hover 不要 `translateY`
- 用 Mobius token：`--surface-control`、`--surface-control-hover`、`--surface-active`、`--border-default`、`--text-muted`、`--text-primary`、`--accent-primary`
- 7 个 icon + 右侧 ⋯ / × 必须在 230px 内可读，不要挤成看不见的点

### F. 测试（必须跑到通过）

```bash
node --test mobius/frontend/tests/workbench-simplification-contract.test.js
node --test mobius/frontend/tests/session-tool-context.test.ts
node --test mobius/frontend/tests/p2-4-surface-hierarchy-contract.test.js
node --test mobius/frontend/tests/workbench-navigation.test.js
```

若仓库惯例不同，按现有 frontend tests 跑。相关失败必须修到通过。

必须继续成立：

- 7 个 tab id 和 7 个中文主标签仍在 `session-tool-drawer.tsx`
- 宽屏 230px 可折叠 Drawer
- editor 仍是可见一级 tab
- 非 shell 仍 `renderAdvancedSessionActions('menu')`
- 关闭回来源、source/object 展示
- files/diff 内容不得再出现 `更多会话动作` 这个 `<details>`
- Drawer header 必须有「更多会话动作」overflow
- overflow variant 存在，且不重复一级入口

## 不要动

- `examples/CodexMonitor/**`
- backend / harness / SSE / Stop / 发送协议
- `ChatArea` 实例稳定性、Send/Stop 分离
- 不要新建第二套 Chat
- 不要 commit、不要改 git config

## 验收

完成后在最后一条消息用中文列出：改了哪些文件、交互如何变简单、跑了哪些测试、还有什么没做。
