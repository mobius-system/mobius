#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
CLI="$SCRIPT_DIR/.research_blackboard_cli"
[[ -f "$CLI" ]] || CLI="$SCRIPT_DIR/research_blackboard_cli.js"
exec node "$CLI" read "$@"
